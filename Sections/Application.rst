===========
Application
===========

Creating a Namespace
====================

Use an instance of `Ember.Application` to create a global namespace
and set up the main Ember.js events.

.. literalinclude:: ../demo/app.js